# Local Contentful configuration
# Found in Contentful: Settings > API keys > Content delivery / preview tokens
CONTENTFUL_SPACE_ID=
CONTENTFUL_ACCESS_TOKEN=

# Local HMAC signing key for password-protected project cookies
# Generate with: openssl rand -hex 32
PROJECT_AUTH_SECRET=

pre-commit:
  commands:
    check:
      run: bunx biome check --write --staged --no-errors-on-unmatched
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}

pre-commit:
  parallel: true
  jobs:
    - name: biome-check
      run: bunx biome check --write --staged --no-errors-on-unmatched
      stage_fixed: true

commit-msg:
  jobs:
    - name: commitlint
      run: bunx commitlint --edit {1}

name: paul protected release
on:
  workflow_call:
    inputs:
      target_sha:
        type: string
        default: ''
      operation:
        type: string
        default: release
      restore_release:
        type: string
        default: ''
permissions:
  contents: read
jobs:
  release:
    # Publication lock. Removal requires separately reviewed activation, NOT a variable toggle.
    if: ${{ false }}
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    environment: production-portfolio
    concurrency:
      group: portfolio-production
      cancel-in-progress: false
    permissions:
      contents: read
      actions: read
      id-token: write
    env:
      GH_TOKEN: ${{ github.token }}
      RELEASE_ENVIRONMENT: production-portfolio
      TARGET_SHA: ${{ inputs.target_sha }}
      RESTORE_RELEASE: ${{ inputs.restore_release }}
      OPERATION: ${{ inputs.operation }}
    steps:
      - name: Check out trusted current verification harness
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.workflow_sha }}
          path: verification-harness
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 24.20.0
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
        with:
          bun-version: 1.4.2
      - name: Root-only tools without sibling lifecycle execution
        working-directory: verification-harness
        run: bun install --frozen-lockfile --ignore-scripts
      - name: Pinned Lighthouse browser before credentials
        run: |
          curl --fail --silent --show-error --location https://storage.googleapis.com/chrome-for-testing-public/152.0.7977.77/linux64/chrome-linux64.zip -o "$RUNNER_TEMP/chrome.zip"
          unzip -q "$RUNNER_TEMP/chrome.zip" -d "$RUNNER_TEMP/chrome"
          echo "CHROME_PATH=$RUNNER_TEMP/chrome/chrome-linux64/chrome" >> "$GITHUB_ENV"
      - name: Select immutable release from trusted workflow context
        working-directory: verification-harness
        run: python3 scripts/release/request.py paul "$OPERATION" "$RUNNER_TEMP/selected.json"
      - name: Validate configuration before credentials
        id: config
        working-directory: verification-harness
        run: python3 scripts/release/deploy.py check paul --operation "$OPERATION" --selected "$RUNNER_TEMP/selected.json"
      - name: Configure isolated expected-account credentials
        uses: aws-actions/configure-aws-credentials@cbe3b392738ccf3f987d68400dafcf4b0624a56c
        with:
          role-to-assume: ${{ steps.config.outputs.roleArn }}
          aws-region: us-east-1
          allowed-account-ids: '658271954302'
          audience: sts.amazonaws.com
          unset-current-credentials: true
          role-duration-seconds: 10800
      - name: Serialized source or byte promotion, smoke, recovery and CAS persistence
        working-directory: verification-harness
        run: python3 scripts/release/deploy.py deploy paul --operation "$OPERATION" --selected "$RUNNER_TEMP/selected.json"
      - name: Scan production diagnostics
        if: always()
        id: reports
        run: python3 verification-harness/scripts/release/reports.py paul verification-harness release-reports production
      - name: Retain scanned production diagnostics
        if: always() && steps.reports.outcome == 'success'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: paul-${{ github.run_id }}-${{ github.run_attempt }}-production-diagnostics
          path: release-reports/
          retention-days: 90
          if-no-files-found: error

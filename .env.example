# Contentful CMS credentials
# Found in Contentful dashboard: Settings > API keys > Content delivery / preview tokens
NEXT_PUBLIC_CONTENTFUL_SPACE_ID=
NEXT_PUBLIC_CONTENTFUL_ACCESS_TOKEN=

# HMAC signing key for password-protected project cookies
# Generate with: openssl rand -hex 32
PROJECT_AUTH_SECRET=
